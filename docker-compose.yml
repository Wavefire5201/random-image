services:
  random-image:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: random-image
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./images:/images:ro
    environment:
      - NODE_ENV=production
